node_modules/
dist/
*.log
.DS_Store

# Local Claude Code session state (scheduled tasks, locks)
.claude/

# --- Self-host (running Sparra on itself) ---
# In a normal *target* project these are the committed source of truth. But when
# Sparra runs on THIS repo, they are throwaway runtime state — don't commit them.
# (Anchored to the root so any same-named files deeper in the tree are unaffected.)
.sparra/
/PLAN.md
/HOLDOUT.md
/CODEBASE_MAP.md
/CHANGELOG.md
/prototypes/
# git worktrees are created as siblings (../Sparra-build-*), outside this repo.
